plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.carpe.microlisto'
    compileSdk 34

    defaultConfig {
        applicationId "com.carpe.microlisto"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildFeatures {
        viewBinding true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }

    packagingOptions {
        resources {
            excludes += ['META-INF/DEPENDENCIES', 'META-INF/LICENSE', 'META-INF/LICENSE.txt', 'META-INF/NOTICE', 'META-INF/NOTICE.txt']
        }
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'androidx.fragment:fragment-ktx:1.8.4'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.4'
    implementation 'androidx.lifecycle:lifecycle-viewmodel-ktx:2.8.4'
    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1'

    implementation 'net.java.dev.jna:jna:5.13.0@aar'
    implementation 'com.alphacephei:vosk-android:0.3.47'

    implementation 'com.microsoft.onnxruntime:onnxruntime-android:1.20.0'

    implementation 'com.google.ai.edge.litert:litert:1.4.2'

    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
}

// ============================================================
// Descarga de modelos para empaquetarlos en assets/
// Se ejecuta antes de cada compilación. Los modelos que ya
// estén descargados no se vuelven a descargar.
// ============================================================

def dirModelos = file("$buildDir/modelos")
def dirAssets = file("src/main/assets")

tasks.register("descargarVosk") {
    doLast {
        dirModelos.mkdirs()
        def zipVosk = new File(dirModelos, "vosk-es.zip")
        if (!zipVosk.exists() || zipVosk.length() < 1000000) {
            println "Descargando modelo Vosk español..."
            ant.invokeMethod("get", [
                src   : "https://alphacephei.com/vosk/models/vosk-model-small-es-0.42.zip",
                dest  : zipVosk,
                verbose: true
            ])
        } else {
            println "Modelo Vosk ya descargado, se omite."
        }
    }
}

tasks.register("descargarSilero") {
    doLast {
        dirModelos.mkdirs()
        def onnxSilero = new File(dirModelos, "silero_vad.onnx")
        if (!onnxSilero.exists() || onnxSilero.length() < 100000) {
            println "Descargando modelo Silero VAD..."
            ant.invokeMethod("get", [
                src   : "https://huggingface.co/runanywhere/silero-vad-v5/resolve/main/silero_vad.onnx",
                dest  : onnxSilero,
                verbose: true
            ])
        } else {
            println "Modelo Silero ya descargado, se omite."
        }
    }
}

tasks.register("copiarModelosAAssets", Copy) {
    dependsOn "descargarVosk", "descargarSilero"
    doFirst {
        dirAssets.mkdirs()
        // Limpiar copias antiguas para que no se acumulen
        delete file("$dirAssets/vosk-model-small-es-0.42")
        delete file("$dirAssets/silero_vad.onnx")
    }
    // Silero: copia directa del archivo
    from("$dirModelos/silero_vad.onnx") {
        into "$dirAssets"
    }
    // Vosk: descomprimir el ZIP dentro de assets/
    from({ zipTree("$dirModelos/vosk-es.zip") }) {
        into "$dirAssets"
    }
}

tasks.named("preBuild") {
    dependsOn "copiarModelosAAssets"
}